# Setup

The base setup installs only common requirements.

Large AI components such as Ollama, local models, PyTorch-heavy environments, and large datasets are installed only when a specific lab needs them.

Files:

- `setup.sh` — base environment setup
- `check_environment.sh` — environment and storage check
- `uninstall.sh` — removes the project's local lab directory

Read scripts before running them.
